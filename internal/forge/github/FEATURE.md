# GitHub Forge

Lets `human` open GitHub pull requests, so an AI agent can hand finished work off for review without leaving the CLI. This is GitHub's code-host (forge) side, kept separate from its issue tracker.

- Open a pull request between two branches
- Target any base branch you choose
- Defaults the repository from the git origin
- Returns the created pull request URL
- Works with GitHub Enterprise endpoints
