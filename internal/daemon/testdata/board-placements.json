{
 "doc": "Places a card can sit that no state in internal/pipelinefsm/pipeline-fsm.json describes. Found by driving the real DeriveBoardCard over the real marker vocabulary and comparing what comes out against what the machine claims. Widening the document until nothing is left over is the move that would make the machine agree with anything, so an entry stays here until something other than tidiness answers it. The three `queued` entries were answered and are gone: the criterion was whether the placement can leave an item stuck with nothing watching it, which is what the pipeline exists to prevent, and queued is exactly that \u2014 so it became a state, with the recovery gap filed as SC-3865 rather than described away. The two below do not meet that criterion, which is why they are still here rather than closed for symmetry.",
 "bound": "A LOWER BOUND. An entry here is real; an absence proves nothing. What no longer bounds it is thread DEPTH: this said a placement reachable only from three markers could not appear, and that was an assumption. Measured 2026-08-09 \u2014 the placement set saturates at two. All 36^3 ordered triples yield the same 27 placements pairs do, and a one-off probe of all 36^4 quadruples added none either; triples are now enumerated by the shipped check so a change that makes a third marker matter fails it. What still bounds it is marker SHAPE: every header is seeded with one body and the options block with one stage-and-count, so a placement that needs a particular body cannot appear.",
 "unclaimed": {
  "hidden/": {
   "reached_by": "a ticket whose tracker status is done or closed",
   "note": "Closing a ticket takes it off the board from ANY state, and reaper.md treats close as cancellation \u2014 it stops the ticket's agents. So it is a transition out of every state at once, fired from outside the marker bus, recorded in no comment. That is why fifty-nine replayed histories never surfaced it and a state-space enumeration does: the two measurements are blind to different things. Since SC-4151 the running case is no longer silent: reconcileOrphanedAgents posts [human:run-cancelled] naming the stage and agent as it stops each one, so a close that interrupted work leaves a trail even though the placement itself is still fired from outside the marker bus.",
   "measured": "2026-08-08, every closed ticket on the PM tracker (382, no sampling). Each was replayed through DeriveBoardCard as if still open, to find where the card was when the close took it off the board. 125 carried no markers at all \u2014 never in the pipeline. 197 were closed AT REST, on a terminal the machine already declares: 173 merged, 13 resolved-no-fix, 11 nothing-to-do. The remaining 60 were closed OUT OF a non-terminal state, which is the transition the document does not have.",
   "shape_of_those_60": "35 from `stopped` (30 done/failed, 3 implementation/failed, 2 planning/failed) \u2014 a person giving up on a card the machine had already given up on. 16 from a state that was WAITING for a gesture rather than working (7 reviewed, 5 handed-off, 4 planned). 5 from backlog. Only 4 from a RUNNING state, where closing kills a live agent: SC-110, SC-296, SC-1865, SC-2400.",
   "era": "Rates over tickets that entered the pipeline, by ticket-number band: SC-<1000 29/99, SC-1000s 13/66, SC-2000s 14/55, SC-3000s 4/37. Declining but present throughout, so this is NOT retired behaviour \u2014 the `legacy` question was asked and answered against the numbers rather than assumed. In the current band all four are `stopped` after a failed deploy, and none is a close from a running state.",
   "not_known": "Why each was closed. The tracker records that a person closed it, not whether the work was abandoned, shipped another way, or folded into another ticket \u2014 and SC-1290's and SC-1692's threads suggest at least the first two both happen. Nothing here says the missing edge should be added; it says how often something walks through the gap where one would be."
  },
  "ideas/": {
   "reached_by": "a ticket carrying the idea label",
   "note": "The machine's initial state is `filed`, and idea-to-PM promotion is a label swap rather than a marker, so an idea is arguably before the machine rather than missing from it. Recorded because the enumeration cannot tell 'deliberately outside' from 'left out' \u2014 only a person can.",
   "known": "Read in code 2026-08-09, three facts. (1) Promotion posts NO marker: RunIdeaPromote (cmd/cmdauto/semantic.go) is one EditIssue with RemoveLabels and nothing else, so ideas -> filed is invisible to the marker bus in exactly the way close is \u2014 which is why replaying markers can never see this edge, and why it took a state-space enumeration to surface it. (2) Membership is the label and only the label: DeriveBoardCard returns the Ideas stage before it looks at a single comment, deliberately (board_state.go), so an idea-labelled ticket that somehow carries pipeline markers renders as an idea and its real pipeline position is hidden. (3) The word `idea` appears NOWHERE in pipeline-fsm.json, while `filed` claims board (backlog, \"\") unconditionally \u2014 so for an idea-labelled ticket that state's `holds` is satisfied and its `board` is wrong.",
   "not_known": "How often (2) actually happens \u2014 whether any idea-labelled ticket carries pipeline markers, which is the number that would say if the hiding is theoretical or real. It needs the label on every ticket, which the local replay corpus does not record (it stores key and markers only) and the tracker was vault-locked when this was written. This is the one of the three still unmeasured against real tickets."
  }
 }
}
